version: "3.8"

services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    environment:
      - OLLAMA_HOST=0.0.0.0
    volumes:
      - ollama-data:/root/.ollama
    ports:
      - "11434:11434"

  rag-api:
    build: .
    container_name: smart-rag-api
    depends_on:
      - ollama
    environment:
      - DOCS_DIR=/docs
      - DB_DIR=/data/chroma_db
      - OLLAMA_URL=http://ollama:11434
      - OLLAMA_MODEL=qwen2.5:7b
      - IMAGE_DIR=/data/extracted_images
      - TABLE_DIR=/data/extracted_tables
      # 👇 여기도 맞춰주면 깔끔
      - HF_HOME=/data/hf_cache
      - TRANSFORMERS_CACHE=/data/hf_cache
      - SENTENCE_TRANSFORMERS_HOME=/data/hf_cache
    volumes:
      - ./docs:/docs:ro
      - ./data/chroma_db:/data/chroma_db
      - ./data/extracted_images:/data/extracted_images
      - ./data/extracted_tables:/data/extracted_tables
      # 👇 캐시를 호스트와 공유해서 다음부터는 다시 안 받게
      - ./data/hf_cache:/data/hf_cache
    ports:
      - "8000:8000"

volumes:
  ollama-data:
