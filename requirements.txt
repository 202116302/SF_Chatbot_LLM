# 1. API(서버)
fastapi
starlette==0.48.0
uvicorn==0.37.0
python-multipart==0.0.20   # /rag/upload 같은 PDF 업로드용

# 2. RAG 코어
langchain==0.3.27
langchain-core==0.3.79
langchain-community==0.3.31
langchain-text-splitters==0.3.11

# 3. LLM 연동
langchain-ollama==0.3.10
ollama==0.6.0              # (LangChain이 대신 불러줘도 되지만 있으면 편함)

# 4. 임베딩/벡터DB
langchain-huggingface==0.3.1
chromadb==1.1.1
sentence-transformers==5.1.1

# 5. PDF 로더
pypdf==6.1.1

# 6. 모델/토치 쪽 (sentence-transformers가 사실상 요구)
torch==2.8.0
transformers==4.57.0
safetensors==0.6.2

# 7. 공통/의존성
requests==2.32.5
pydantic==2.12.0
typing_extensions==4.15.0
orjson==3.11.3     # FastAPI 성능 조금 더 내고 싶으면
